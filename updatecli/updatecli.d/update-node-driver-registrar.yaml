---
name: "Update node-driver-registrar version"

sources:
  vsphere-csi-driver:
    name: Get latest vsphere-csi-driver version
    kind: githubrelease
    spec:
      owner: kubernetes-sigs
      repository: vsphere-csi-driver
      token: '{{ requiredEnv .github.token }}'
      typefilter:
        release: true
        draft: false
        prerelease: false
      versionfilter:
        kind: semver

  node-driver-registrar:
    name: Get node-driver-registrar version from vsphere-csi-driver manifest
    kind: file
    spec:
      file: 'https://raw.githubusercontent.com/kubernetes-sigs/vsphere-csi-driver/{{ source "vsphere-csi-driver" }}/manifests/vanilla/vsphere-csi-driver.yaml'
      matchpattern: 'registry\.k8s\.io/sig-storage/csi-node-driver-registrar:(v[0-9]+\.[0-9]+\.[0-9]+)'
      replacepattern: '$1'

targets:
  tagfile:
    name: "Bump to node-driver-registrar version used by vsphere-csi-driver in TAG"
    kind: file
    sourceid: node-driver-registrar
    scmid: default
    disablesourceinput: true
    spec:
      file: TAG
      matchpattern: '(?m)^(.+)$'
      replacepattern: '{{ source "node-driver-registrar" }}'

scms:
  default:
    kind: github
    spec:
      token: '{{ requiredEnv .github.token }}'
      username: '{{ .github.username }}'
      user: '{{ .github.username }}'
      email: '{{ .github.email }}'
      owner: '{{ .github.owner }}'
      repository: '{{ .github.repository }}'
      branch: '{{ .github.branch }}'

actions:
  default:
    title: 'Bump node-driver-registrar version to {{ source "node-driver-registrar" }}'
    kind: github/pullrequest
    spec:
      merge:
        strategy: auto
      labels:
        - updatecli
    scmid: default
